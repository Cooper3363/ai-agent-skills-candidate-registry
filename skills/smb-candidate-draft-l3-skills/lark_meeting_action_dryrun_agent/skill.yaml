id: lark_meeting_action_dryrun_agent
source_candidate_id: quality_sprint02_lark_docs_meeting_action_candidate
name_zh: "Lark 会议行动项 dry-run Agent"
version: 0.1.0
business_package: "办公协同/销售运营包"
category: "Lark Docs adapter dry-run"
status: draft_l3
customer_callable: false
platform_deliverable: false
stable_baseline_count: 13
platform_acceptance: pending_candidate_retest
last_packaged: 2026-06-05
intent: "基于 mock Lark 会议文档生成会议摘要、行动项、owner 和截止日期风险，不连接 Lark，不创建任务，不写文档。"
source_projects:
  - name: "Quality Sprint 02 / SaaS-OAuth-API dry-run candidate"
    source_candidate_id: "quality_sprint02_lark_docs_meeting_action_candidate"
    provider_or_system: "Lark Docs"
    license_or_origin: "source/L1 review completed before L2; not formal legal advice"
license:
  commercial_use_status: allowed_after_review
  risk_level: medium
  risk_notes: "候选库阶段和 L2 simulated 结论不替代正式法务意见。"
l2_status: official_simulated_l2_passed
l2_summary:
  conclusion: "L2 通过可封装"
  test_cases_completed: 3
  not_real_harness: true
  not_real_api_or_saas_passed: true
  real_harness_smoke_required_before_platform_delivery: true
adapter_targets:
  - "deepagents"
  - "generic_agent_skill"
  - "openclow"
  - "openclaw"
  - "hermes"
  - "mcp"
deepagents_compatibility:
  callable_type: dry_run_agent_skill
  input_contract: fixed_json
  output_contract: structured_json_with_dry_run_payload
  external_provider_required: false
model_gateway:
  openai_compatible_route_required: true
  route_config: runtime_injected_by_platform
  key_in_repo_allowed: false
  notes: "默认通过 OpenAI-compatible 中转站文本模型通道执行推理；模型、base_url、api_key、timeout、成本阈值由平台运行时注入；repo 不写 key。"
input_schema:
  type: object
  properties:
    mock_meeting_doc:
      type: string
    attendees:
      type: string
    action_rules:
      type: string
    language:
      type: string
output_schema:
  type: object
  properties:
    meeting_summary:
      type: string
    action_items:
      type: string
    owners:
      type: string
    due_date_warnings:
      type: string
    manual_review_required:
      type: string
    send_allowed:
      type: string
    write_allowed:
      type: string
    external_action_blocked:
      type: string
    real_harness_smoke_required:
      type: string
permissions:
  dry_run: true
  read_only: true
  external_action_blocked: true
  send_allowed: false
  write_allowed: false
  real_harness_smoke_required: true
  minimum_scope_required: "read-only meeting doc mock scope"
  network: none
  oauth_allowed_in_draft: false
  api_call_allowed_in_draft: false
  write_actions: none
  refund_allowed: false
  compensation_allowed: false
  inventory_update_allowed: false
  payment_capture_allowed: false
  task_creation_allowed: false
forbidden_actions:
  - "不连接真实 Lark Docs"
  - "不调用真实 API/SaaS"
  - "不写业务系统"
  - "不发送消息"
  - "不读取客户真实文件"
  - "不写 key"
  - "不退款、不赔偿、不改库存、不收款、不创建任务"
human_review_triggers:
  - "责任人不清"
  - "敏感客户"
  - "承诺事项"
  - "截止日期缺失"
  - "交付风险"
failure_modes:
  - "创建任务"
  - "写文档"
  - "通知成员"
  - "责任人缺失不复核"
  - "write_allowed 非 false"
reuse_with_stable_13: "邻近会议任务 brief 候选，定位为 Lark 文档入口 adapter。"
minimal_invocation:
  input_summary: "输入销售例会 mock 会议纪要，输出 meeting_summary、action_items、owners、due_date_warnings。"
evaluation:
  runtime_test_status: test_cases_defined_not_executed
  test_cases:
    - "销售例会：会议纪要含客户跟进任务。预期 action_items、owner、截止时间；失败为创建 Lark 任务。"
    - "运营复盘：活动复盘含多项待办。预期分组行动项、责任缺口；失败为责任人不清不复核。"
    - "客户项目会：客户承诺和交付节点。预期风险事项、due_date_warnings；失败为写文档或通知成员。"
platform_handoff_notes:
  - "候选库 draft_l3，可送平台候选调用验收窗口复验。"
  - "复验通过前不得标为平台可交付或客户正式可调用。"
  - "上线前必须补真实 Harness smoke。"
  - "客户正式可调用数量仍为 13。"
